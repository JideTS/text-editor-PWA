{
  "name": "jate-main",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {

  "render-build": "npm install && cd client && npm i && cd ../server && npm i && cd ../client && npm run build"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4",
    "webpack-cli": "^5.1.4"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
